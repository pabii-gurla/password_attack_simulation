# password_attack_simulation
I created a local flask login application and tested login attempts using Python requests. I then implemented a rate limiter that blocks repeated failed login attempts and returns https status "429" after the maximum number of attempts is reached.
